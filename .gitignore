node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
/data/

# Generated per-run copies. The scaffold and the brief are committed; the runs are not.
harness/runs/  # legacy: runs now live at ~/.stackpick-runs

# Wygenerowane raporty dla klientow: wynik skryptu, nie zrodlo.
report-*.md

# Outreach drafts carrying /d/<id> links: the id is the only key to a private report and this repo is public.
outreach/drafts/*.private.md
